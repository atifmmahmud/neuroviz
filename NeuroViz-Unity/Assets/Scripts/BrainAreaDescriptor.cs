using UnityEngine;

public class BrainAreaDescriptor : MonoBehaviour
{
    public string Name;
    public string Description;


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
